{
  "name": "apap-automationtest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "regression": "npx playwright test --grep @regression --project chromium --workers=1 --retries=2",
    "smoke": "npx playwright test --grep @regression --project chromium --workers=1 --retries=2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.9"
  }
}
